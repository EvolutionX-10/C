#include <stdio.h>

int genNum(int a, int b);
int main() {
	int num, t;
	printf("Enter a number and number of terms: ");
	scanf(" %d %d", &num, &t);

	int sum = 0;
	for (int i = 0; i < t; i++) {
		
	}
	return 0;
}

int genNum(int a, int b) {
	for (int i = 0; i < b; i++) {
		
	}
}
